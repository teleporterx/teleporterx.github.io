```bash
git clone https://github.com/teleporterx/teleporterx.github.io.git
sudo bundle
# build and test locally: development
sudo bundle exec jekyll s # local server for testing
# deploy (for other domains)
sudo bundle exec jekyll build
# push to github
git add.
git commit -m "message"
git push origin main
```